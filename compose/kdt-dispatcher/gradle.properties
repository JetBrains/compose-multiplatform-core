# This module is JVM/desktop-only and has no Android artifact, so it does not
# participate in the compose android -> androidx.compose artifact redirection
# configured in ../gradle.properties. Disable it here.
artifactRedirection.targetNames=
